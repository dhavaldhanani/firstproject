$(document).ready(function() {
 
  $(".owl-carousel").owlCarousel({
    items:1,
    margin:20,
    // loop:true,
    // center:true,
    mouseDrag:false,
    merge:true,
    // stagePadding:100,
    // rewind:true,
    // slideBy:3,
    startPosition:0,
    rewind:true,
    // slideTransition:'cubic-bezier(.36,1.56,.7,.05)',
    // dots:false,
    // dotsEach:true,
    autoplay:true,
    autoplayTimeout:2000,
    // autoplayHoverPause:true,
    // autoplaySpeed:7000,
  });
});   